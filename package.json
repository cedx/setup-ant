{
	"bugs": "https://github.com/cedx/setup-ant/issues",
	"description": "Set up your GitHub Actions workflow with a specific version of Apache Ant.",
	"license": "MIT",
	"name": "@cedx/setup-ant",
	"private": true,
	"repository": "cedx/setup-ant",
	"type": "module",
	"version": "2.0.0",
	"author": {
		"email": "cedric@belin.io",
		"name": "Cédric Belin",
		"url": "https://belin.io"
	},
	"bin": {
		"setup_ant": "./bin/setup_ant.cjs"
	},
	"dependencies": {
		"@actions/core": "^1.10.1",
		"@actions/tool-cache": "^2.0.1",
		"semver": "^7.6.0"
	},
	"devDependencies": {
		"@types/node": "^20.11.16",
		"@types/semver": "^7.5.6",
		"@typescript-eslint/eslint-plugin": "^6.21.0",
		"@typescript-eslint/parser": "^6.21.0",
		"del": "^7.1.0",
		"esbuild": "^0.20.0",
		"eslint": "^8.56.0",
		"execa": "^8.0.1",
		"typedoc": "^0.25.7",
		"typescript": "^5.3.3"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"exports": {
		"types": "./lib/index.d.ts",
		"import": "./lib/index.js"
	},
	"files": [
		"lib/",
		"src/"
	],
	"imports": {
		"#setupAnt": {
			"types": "./lib/index.d.ts",
			"import": "./lib/index.js"
		}
	},
	"keywords": [
		"action",
		"ant",
		"ci",
		"github",
		"ivy",
		"java",
		"test",
		"workflow"
	],
	"scripts": {
		"build": "tsc --project src/tsconfig.json && esbuild \"--banner:js=#!/usr/bin/env node\" --bundle --legal-comments=none --log-level=warning --minify --outfile=bin/setup_ant.cjs --platform=node src/cli.ts",
		"clean": "node tool/clean.js",
		"dist": "npm run clean && npm run build && node tool/dist.js && git update-index --chmod=+x bin/setup_ant.cjs",
		"doc": "typedoc --options etc/typedoc.js && node tool/doc.js",
		"lint": "npm run build && tsc --project tsconfig.json && eslint --config=etc/eslint.cjs src test tool",
		"release": "npm run dist && node tool/publish.js",
		"test": "npm run build && node --test --test-reporter=spec"
	}
}
